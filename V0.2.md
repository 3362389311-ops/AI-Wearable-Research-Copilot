# AI Wearable Research Copilot v0.2

## 本版本完成内容

v0.2 已完成最小可用 CLI 流程：

`用户输入产品资料 → DeepSeek API → JSON 输出 → Python 解析 → result.json 保存`

当前固定输出四类信息：

- `features`：产品特征
- `evidence`：原始资料证据
- `risks`：风险或未验证信息
- `questions`：后续需要继续研究的问题

## 当前工程能力

- 使用 `.env` 保存 DeepSeek API Key
- CLI 接收产品资料
- 调用 DeepSeek API
- 使用结构化 JSON 输出
- `json.loads()` 转为 Python `dict`
- 自动保存 `result.json`

## 下一步

加入异常处理、JSON 校验、retry 和日志。
